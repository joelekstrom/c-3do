language: c
compiler:
- clang
- gcc

before_script:
  - sudo apt-get -qq update
  - sudo apt-get install -y libsdl2-dev
  - sudo apt-get install -y libsdl2-image-dev
  - mkdir bin
  - cd bin
  - cmake ..

script: make